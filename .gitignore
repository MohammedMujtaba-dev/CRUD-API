# Node modules (prevent uploading dependencies)
node_modules/

# Logs (avoid unnecessary log files)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (keep API keys and database credentials private)
.env

# Database files (prevent uploading database files)
*.sqlite
*.sqlite-journal
/data/

# Editor-specific settings (prevent user-specific IDE settings)
.vscode/
.idea/
.DS_Store
Thumbs.db

# Build files (ignore compiled or built files)
dist/
build/
